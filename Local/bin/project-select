#!/usr/bin/sh

layout=dev
project=$(find $HOME/Development $HOME/Development/Playad -maxdepth 1 -mindepth 1 -type d | rofi -dmenu)

[[ -n $project ]] || exit

exec terminator -T $(basename $project) -l $layout --working-directory=$project
