#!/bin/sh

prefix=$HOME/Development

project=$(find $prefix $prefix/Playad -maxdepth 1 -mindepth 1 -type d | cut -f 5- -d '/' | rofi -dmenu)

[[ -n $project ]] || exit

project_dir=$prefix/$project
cat << EOF | last-command
export CD_TO=$project_dir
exec st -e ~/Local/bin/dev
EOF
