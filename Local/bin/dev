#!/bin/sh
if ! machinectl list | grep "^dev" > /dev/null; then
  sudo systemctl start systemd-nspawn@dev
fi

sudo machinectl shell --uid=$UID --setenv=DISPLAY=$DISPLAY --setenv=CD_TO=$PWD dev /usr/bin/zsh
